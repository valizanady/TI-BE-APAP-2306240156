apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tourpackage-be-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306240156-be.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tourpackage-be-service
                port:
                  number: 80
